@using System.Web.Mvc.Html
@if(Request.IsAuthenticated) {
    <text>Witaj <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Wylogowanie", "LogOff", "Account") ]</text>
}
else {
    @:[ @Html.ActionLink("Logowanie", "LogOn", "Account") ]
}
